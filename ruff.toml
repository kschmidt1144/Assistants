# Monorepo-wide Python lint config (core-py + app backends).
target-version = "py313"
line-length = 110
extend-exclude = [".npmcache"]

[lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
# E501: long lines are fine; B008: FastAPI's File()/Depends() defaults are idiomatic.
ignore = ["E501", "B008"]
